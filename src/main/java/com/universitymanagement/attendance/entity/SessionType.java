package com.universitymanagement.attendance.entity;

/** What kind of meeting a session is. Shown on the register, not scored differently. */
public enum SessionType {
    LECTURE,
    LAB,
    TUTORIAL,
    SEMINAR,
    EXAM,
    OTHER
}
